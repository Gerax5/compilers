do { 
        const x: integer = 1;
} while (true);

switch (true) {
            case true:
                break;   // permitido dentro de switch
            default:
                // nada
        }